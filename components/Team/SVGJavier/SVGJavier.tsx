import * as React from "react";
import PropTypes from "prop-types";

type SVGJavierProps = {
  //
};

const SVGJavier: React.FC<SVGJavierProps> = () => {
  return <img src="/assets/img/javier.jpeg"></img>;
};

SVGJavier.propTypes = {
  //
};

export default SVGJavier;
